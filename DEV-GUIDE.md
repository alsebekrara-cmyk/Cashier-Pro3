# 🚀 دليل البدء السريع - للمطورين

## 📦 التثبيت والتشغيل في 3 خطوات

### الطريقة 1: استخدام ملفات BAT (الأسهل)

```bash
# 1. تثبيت المكتبات
install.bat

# 2. تشغيل التطبيق
start.bat

# 3. بناء التطبيق (اختياري)
build.bat
```

### الطريقة 2: استخدام npm

```bash
# 1. تثبيت المكتبات
npm install

# 2. تشغيل التطبيق
npm start

# 3. بناء التطبيق
npm run build-win
```

---

## 📁 الملفات الأساسية

| الملف | الوصف |
|------|-------|
| `main.js` | ملف Electron الرئيسي - يدير النوافذ والطباعة |
| `splash.html` | شاشة الترحيب مع معلومات الشركة |
| `index.html` | واجهة التطبيق الرئيسية |
| `preload.js` | سكريبت التحميل المسبق - واجهة IPC |
| `package.json` | إعدادات المشروع والمكتبات |

---

## 🖨️ نظام الطباعة

### واجهات الطباعة المتاحة (IPC)

```javascript
// الطباعة المباشرة (بدون معاينة)
window.electronAPI.printSilent({
    pageSize: { width: 80000, height: 200000 }
});

// الطباعة مع معاينة
window.electronAPI.printPreview();

// طباعة إلى PDF
window.electronAPI.printToPDF({ pageSize: 'A4' });

// الحصول على قائمة الطابعات
window.electronAPI.getPrinters();

// طباعة على طابعة محددة
window.electronAPI.printToPrinter('اسم_الطابعة', {
    silent: true
});

// الاستماع للنتائج
window.electronAPI.onPrintResult((result) => {
    if (result.success) {
        console.log('تمت الطباعة بنجاح');
    }
});
```

### إعدادات الطباعة المحفوظة

جميع إعدادات الطباعة محفوظة في:
```javascript
localStorage.getItem('printerSettings')
```

الإعدادات تشمل:
- نوع الطابعة
- حجم الورق
- جميع أحجام الخطوط (6 أحجام مختلفة)
- التنسيق والهوامش
- خيارات العرض
- نص التذييل المخصص

---

## 🎨 تخصيص التطبيق

### تغيير الشعار

استبدل الملفات:
```
Gemini_Generated_Image_w319kww319kww319.png  (للتطبيق)
build/icon.png                               (أيقونة PNG)
build/icon.ico                               (أيقونة Windows)
```

### تغيير معلومات الشركة

عدّل الملفات:
- `splash.html` - شاشة الترحيب
- `package.json` - معلومات المطور
- `LICENSE.txt` - نص الترخيص

---

## 🔧 الأوامر المتاحة

```bash
# تثبيت المكتبات
npm install

# تشغيل التطبيق
npm start

# بناء لـ Windows
npm run build-win

# بناء لجميع الأنظمة
npm run build-all

# تنظيف مجلد البناء
rmdir /s dist
```

---

## 📊 البنية المعمارية

```
Electron App
│
├── Main Process (main.js)
│   ├── إدارة النوافذ
│   ├── معالجة الطباعة
│   └── IPC Handlers
│
├── Renderer Process (index.html)
│   ├── واجهة المستخدم
│   ├── IndexedDB
│   └── localStorage
│
└── Preload Script (preload.js)
    └── Context Bridge API
```

---

## 🐛 تصحيح الأخطاء

### تفعيل أدوات المطور

في `main.js`:
```javascript
if (process.env.NODE_ENV === 'development') {
    mainWindow.webContents.openDevTools();
}
```

أو شغّل:
```bash
set NODE_ENV=development && npm start
```

### سجلات Console

```javascript
// في Renderer Process
console.log('رسالة في واجهة التطبيق');

// في Main Process (main.js)
console.log('رسالة في Electron الرئيسي');
```

---

## 📦 المكتبات المستخدمة

| المكتبة | الإصدار | الغرض |
|---------|---------|-------|
| `electron` | ^28.0.0 | إطار العمل الرئيسي |
| `electron-builder` | ^24.9.1 | بناء التطبيق |
| `electron-store` | ^8.1.0 | تخزين الإعدادات |

---

## 🚀 نشر التطبيق

### 1. اختبار التطبيق
```bash
npm start
```

### 2. بناء التطبيق
```bash
npm run build-win
```

### 3. اختبار التثبيت
- افتح `dist/POS-System-Setup-1.0.0.exe`
- ثبّت التطبيق
- جرّب جميع الوظائف

### 4. التوزيع
- رفع الملف على موقع الشركة
- أو توزيعه مباشرة للعملاء

---

## 📞 الدعم الفني

**شركة الإبداع الرقمي**
- 📱 07813798636
- 💬 واتساب: 07813798636

---

## ✅ قائمة التحقق قبل النشر

- [ ] اختبار جميع الوظائف
- [ ] اختبار الطباعة (حرارية وعادية)
- [ ] اختبار التثبيت والإلغاء
- [ ] التحقق من الأيقونات
- [ ] التحقق من شاشة الترحيب
- [ ] التحقق من معلومات الشركة
- [ ] اختبار على Windows 10/11
- [ ] إنشاء نسخة احتياطية من الكود

---

**صُنع بـ ❤️ - شركة الإبداع الرقمي**
