# نظام قاعدة البيانات - POS System Database

## 📦 الملفات المضافة

تم إضافة نظام قاعدة بيانات محلي كامل للتطبيق:

### 1. `_sdk/data_sdk.js` - قاعدة البيانات الرئيسية
- **التقنية**: IndexedDB (قاعدة بيانات محلية في المتصفح)
- **السعة**: غير محدودة تقريباً (حسب مساحة التخزين المتاحة)
- **الاستمرارية**: البيانات تبقى محفوظة حتى بعد إغلاق المتصفح

#### الوظائف المتاحة:

```javascript
// تهيئة قاعدة البيانات
await window.dataSdk.init(dataHandler);

// إضافة سجل جديد (منتج، تصنيف، مبيعة، إلخ)
const result = await window.dataSdk.create({
    type: 'product',
    product_name: 'منتج جديد',
    // ... بقية البيانات
});

// تحديث سجل موجود
await window.dataSdk.update(recordId, { product_name: 'اسم جديد' });

// حذف سجل
await window.dataSdk.delete(recordId);

// البحث عن سجلات
const products = window.dataSdk.query({ type: 'product' });

// الحصول على جميع البيانات
const allData = window.dataSdk.getAll();

// تصدير البيانات
const jsonData = window.dataSdk.exportData();

// استيراد البيانات
await window.dataSdk.importData(jsonData);
```

### 2. `_sdk/element_sdk.js` - إدارة الإعدادات
- **التقنية**: localStorage
- **الاستخدام**: حفظ إعدادات التطبيق (اسم المتجر، الضريبة، إلخ)

#### الوظائف المتاحة:

```javascript
// تهيئة SDK
await window.elementSdk.init({
    defaultConfig: { store_name: 'متجري', tax_rate: 15 },
    onConfigChange: (config) => console.log('تم تحديث الإعدادات', config)
});

// تحديث الإعدادات
await window.elementSdk.updateConfig({ store_name: 'اسم جديد' });

// الحصول على الإعدادات
const config = window.elementSdk.getConfig();

// إعادة تعيين للقيم الافتراضية
await window.elementSdk.resetConfig();
```

## 🎯 المميزات

### ✅ تخزين دائم
- جميع البيانات تُحفظ محلياً في المتصفح
- لا حاجة لخادم أو اتصال بالإنترنت
- البيانات تبقى حتى بعد إعادة تشغيل الجهاز

### ✅ أداء عالي
- البحث والاسترجاع السريع باستخدام الفهارس
- معالجة غير متزامنة (Async) لعدم تجميد الواجهة

### ✅ آمن وموثوق
- معالجة الأخطاء بشكل كامل
- التحقق من صحة البيانات قبل الحفظ
- رسائل خطأ واضحة

### ✅ سهل الاستخدام
- واجهة برمجية بسيطة وواضحة
- توثيق كامل لجميع الوظائف

## 📊 أنواع البيانات المدعومة

النظام يدعم حفظ جميع أنواع البيانات:

1. **المنتجات** (`type: 'product'`)
   - اسم المنتج، السعر، الكمية، الباركود، التصنيف، إلخ

2. **التصنيفات** (`type: 'category'`)
   - اسم التصنيف، الأيقونة، إلخ

3. **المبيعات** (`type: 'sale'`)
   - تفاصيل البيع، المنتجات المباعة، الإجمالي، التاريخ، إلخ

4. **الديون** (`type: 'debt'`)
   - اسم العميل، المبلغ، المدفوع، المتبقي، إلخ

5. **أي نوع آخر**
   - النظام مرن ويدعم إضافة أي نوع من البيانات

## 🔧 كيفية الاستخدام

### التشغيل
1. افتح الملف `index.html` في المتصفح
2. النظام سيُهيئ قاعدة البيانات تلقائياً
3. ابدأ بإضافة المنتجات والتصنيفات

### النسخ الاحتياطي
```javascript
// تصدير جميع البيانات
const backup = window.dataSdk.exportData();
console.log(backup); // انسخ هذا النص واحفظه في ملف

// استيراد البيانات
await window.dataSdk.importData(backup);
```

### مسح البيانات
```javascript
// حذف جميع البيانات (استخدم بحذر!)
await window.dataSdk.clearAll();
```

## 🐛 استكشاف الأخطاء

### لا تُحفظ البيانات؟
1. تأكد من تشغيل الملفات من خلال خادم (http://localhost أو http://127.0.0.1)
2. تحقق من عدم استخدام وضع التصفح الخاص/المتخفي
3. تأكد من تفعيل JavaScript في المتصفح

### رسالة خطأ في Console؟
- افتح أدوات المطور (F12)
- تحقق من تفاصيل الخطأ في تبويب Console
- جميع الأخطاء تُسجل مع تفاصيل واضحة

## 📝 ملاحظات مهمة

1. **التوافق**: يعمل على جميع المتصفحات الحديثة (Chrome, Firefox, Edge, Safari)
2. **السعة**: يمكن تخزين آلاف السجلات بدون مشاكل
3. **الأمان**: البيانات محلية بالكامل ولا تُرسل لأي خادم
4. **النسخ الاحتياطي**: يُنصح بعمل نسخ احتياطي دوري للبيانات

## 🎉 جاهز للاستخدام!

النظام الآن جاهز بالكامل ويمكنك:
- ✅ إضافة تصنيفات ومنتجات
- ✅ إجراء مبيعات
- ✅ إدارة الديون
- ✅ تتبع المخزون
- ✅ عرض التقارير

**جميع البيانات ستُحفظ تلقائياً وبشكل دائم!** 🚀
